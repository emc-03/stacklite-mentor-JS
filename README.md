# stacklite-mentor-JS
This is a **complete Claude Code–style YAML package**, followed by a **step-by-step guide explaining how each piece works and how you can extend it**.
---
Create a YAML file with agent metadata, model configuration, and versioning
	•	Configure the AI model provider, temperature, and token limits
	•	Define structured memory with a clear schema for stored data
	•	Add external tools using HTTP endpoints and API authentication
	•	Build workflows for intake, qualification, and conditional execution
	•	Use templating to inject stored data into tool payloads
	•	Apply run_if conditions to control branching logic
	•	Test end-to-end flow from data collection to CRM sync and email confirmation
	•	Extend with logging, validation, retries, and additional integrations as needed
